const userReducer = (
    state = { username: '', email: '', socketId: '', userjoindate: '' },
    action
) => {

    switch (action.type) {
        default:
            break
    }

    return state

}

export default userReducer